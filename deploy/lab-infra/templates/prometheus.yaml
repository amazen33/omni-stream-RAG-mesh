apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
  namespace: {{ .Release.Namespace }}
data:
  config.yaml: |
    global:
      scrape_interval: 15s
    scrape_configs:
      - job_name: rag-api
        static_configs:
          - targets: [rag-api:8000]
