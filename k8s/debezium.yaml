apiVersion: v1
kind: ConfigMap
metadata: {name: debezium-connector, namespace: rag}
data:
  connector.json: |
    {"name":"transactions-cdc","config":{"connector.class":"io.debezium.connector.postgresql.PostgresConnector","database.hostname":"${DB_HOST}","database.user":"${DB_USER}","database.password":"${DB_PASSWORD}","database.dbname":"transactions","topic.prefix":"transactions","plugin.name":"pgoutput","publication.autocreate.mode":"filtered","table.include.list":"public.transactions","snapshot.mode":"initial"}}
