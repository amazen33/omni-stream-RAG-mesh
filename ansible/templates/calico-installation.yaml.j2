apiVersion: operator.tigera.io/v1
kind: Installation
metadata:
  name: default
spec:
  calicoNetwork:
    bgp: Disabled
    ipPools:
      - cidr: "{{ kubernetes_pod_subnet }}"
        encapsulation: VXLAN
        natOutgoing: Enabled
        nodeSelector: all()
